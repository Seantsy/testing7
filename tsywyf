  cd text（text为文件夹名）
           git初始化
           git init

   创建gh-pages分支：
          git checkout --orphan gh-pages
  添加文件到暂存区：
         git add text #缓存(某个文件夹或具体文件名）
         git add .      #缓存所有文件
 记录提交信息：
        git commit -m "This is add message"
 关联仓库：
      git remote add origin git@github.com:username/project.git
部署你的项目到github：
    git push -u origin gh-pages
